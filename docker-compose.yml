services:
  postgres:
    image: postgres:16-alpine
    container_name: security_messenger_db
    environment:
      POSTGRES_DB: security_messenger
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
